name: build

# v0.2.0 - windows-2022 + debloat (lite). Tunnel: Tailscale.
# Debloat bersifat best-effort: kalau ada yang diblok (tamper protection),
# job tetap lanjut sampai RDP nyala. Secrets: TS_AUTHKEY, RDP_PASSWORD.

on:
  workflow_dispatch:
    inputs:
      minutes:
        description: "session minutes"
        required: false
        default: "340"

jobs:
  run:
    runs-on: windows-2022
    timeout-minutes: 350
    steps:
      - name: setup
        shell: pwsh
        env:
          PW: ${{ secrets.RDP_PASSWORD }}
        run: |
          if (-not $env:PW) { throw "RDP_PASSWORD empty" }
          Set-ItemProperty 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name fDenyTSConnections -Value 0
          Enable-NetFirewallRule -DisplayGroup 'Remote Desktop'
          Set-ItemProperty 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name UserAuthentication -Value 0
          Set-LocalUser -Name runneradmin -Password (ConvertTo-SecureString $env:PW -AsPlainText -Force)

      - name: lite
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Continue'

          # --- Defender: matikan real-time (best-effort, bisa diblok tamper) ---
          try {
            Set-MpPreference -DisableRealtimeMonitoring $true -ErrorAction Stop
            Set-MpPreference -DisableScriptScanning $true -DisableBehaviorMonitoring $true -DisableIOAVProtection $true -ErrorAction SilentlyContinue
            echo "[lite] defender realtime OFF"
          } catch {
            echo "[lite] defender realtime BLOCKED (tamper) -> pakai exclusion"
            Add-MpPreference -ExclusionPath "C:\Users\runneradmin" -ErrorAction SilentlyContinue
            Add-MpPreference -ExclusionProcess "steam.exe","PixelWorlds.exe" -ErrorAction SilentlyContinue
          }

          # --- Services berat yang aman dimatikan ---
          $svc = @('WSearch','SysMain','DiagTrack','Spooler','MapsBroker','WbioSrvc',
                   'XblAuthManager','XblGameSave','XboxGipSvc','XboxNetApiSvc')
          foreach ($s in $svc) {
            try {
              Stop-Service $s -Force -ErrorAction SilentlyContinue
              Set-Service $s -StartupType Disabled -ErrorAction SilentlyContinue
              echo "[lite] disabled $s"
            } catch { echo "[lite] skip $s" }
          }

          # --- Visual: best performance + matikan wallpaper/font smoothing ---
          try {
            $vfx = 'HKCU:\Software\Microsoft\Windows\CurrentVersion\Explorer\VisualEffects'
            New-Item -Path $vfx -Force | Out-Null
            Set-ItemProperty $vfx -Name VisualFXSetting -Value 2
            Set-ItemProperty 'HKCU:\Control Panel\Desktop' -Name Wallpaper -Value "" -ErrorAction SilentlyContinue
            Set-ItemProperty 'HKCU:\Control Panel\Desktop' -Name FontSmoothing -Value "0" -ErrorAction SilentlyContinue
            Set-ItemProperty 'HKCU:\Control Panel\Desktop' -Name DragFullWindows -Value "0" -ErrorAction SilentlyContinue
            echo "[lite] visual = best performance"
          } catch { echo "[lite] visual tweak skipped" }

          echo "[lite] done"

      - name: net
        shell: pwsh
        env:
          TK: ${{ secrets.TS_AUTHKEY }}
        run: |
          if (-not $env:TK) { throw "TS_AUTHKEY empty" }
          Invoke-WebRequest 'https://pkgs.tailscale.com/stable/tailscale-setup-latest-amd64.msi' -OutFile ts.msi
          Start-Process msiexec.exe -ArgumentList '/i','ts.msi','/quiet','/norestart' -Wait
          $ts = 'C:\Program Files\Tailscale\tailscale.exe'
          for ($i=0; $i -lt 10 -and -not (Test-Path $ts); $i++) { Start-Sleep 3 }
          & $ts up --authkey=$env:TK --hostname=ci-box --accept-routes --timeout=60s
          Start-Sleep 5
          echo "tailnet ip:"
          & $ts ip -4

      - name: run
        shell: pwsh
        run: |
          $m = [int]"${{ github.event.inputs.minutes }}"
          if ($m -le 0 -or $m -gt 345) { $m = 340 }
          for ($i = 1; $i -le $m; $i++) {
            Start-Sleep 60
            if ($i % 15 -eq 0) { echo "ok $i/$m" }
          }
